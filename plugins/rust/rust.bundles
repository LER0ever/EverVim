" EverVim Rust Bundles
Plug 'rust-lang/rust.vim', {'for': ['rust']}
Plug 'racer-rust/vim-racer', {'for': ['rust']}
if count(g:evervim_bundle_groups, 'deoplete')
    Plug 'sebastianmarkow/deoplete-rust', { 'for': ['rust'] }
endif
